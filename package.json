{
  "name": "ai-math-explainers",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "astro": "7.3.2"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "happy-dom": "^20.14.0"
  }
}
